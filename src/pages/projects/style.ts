import { styled } from "@mui/material";

export const Wrapper = styled("div")({
  display: "flex",
  width: "100%",
  maxWidth: 800,
  height: 400,
  padding: 24,
});

export const Title = styled("h1")({
  fontSize: 46,
});
